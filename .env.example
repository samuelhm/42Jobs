POSTGRES_USER=42jobs
POSTGRES_PASSWORD=change_me
POSTGRES_DB=42jobs

# PostgreSQL connection string (for .NET backend)
DATABASE_URL=postgresql://42jobs:change_me@db:5432/42jobs

# JWT Auth
JWT_SECRET_KEY=change_me_min_32_chars

# API keys: configure via Admin panel (http://localhost:3000/admin)
# - AI Services: Admin > AI Services (Google/OpenAI API key)
# - Job Providers: Admin > Job Providers (LinkedIn RapidAPI host + key)
